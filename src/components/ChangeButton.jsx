import React from 'react'
import { Button } from 'react-bootstrap'

export const ChangeButton = () => {
  return (
    <Button variant="outline-primary" size='sm' className='position-absolute start-50 translate-middle mt-4'>←→</Button>
  )
}
